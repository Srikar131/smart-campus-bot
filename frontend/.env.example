# Frontend configuration
# In local development you can leave this empty — the CRA dev-server proxy
# (see "proxy" in package.json) forwards /api requests to the backend.
# For a production build, set this to your deployed backend origin, e.g.
# REACT_APP_BACKEND_URL=https://api.yourcampus.edu
REACT_APP_BACKEND_URL=
